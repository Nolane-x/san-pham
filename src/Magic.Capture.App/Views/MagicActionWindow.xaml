<Window
    x:Class="Magic.Capture.App.Views.MagicActionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Magic Capture Desktop — Magic">

    <Grid Padding="18" RowSpacing="12" ColumnSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.05*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Magic" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Padding="7,3"><TextBlock Text="PRO" Foreground="White" FontWeight="SemiBold"/></Border>
            <TextBlock x:Name="ProviderBadge" VerticalAlignment="Center" Opacity="0.7" Text="No provider selected"/>
            <ProgressRing x:Name="BusyRing" Width="20" Height="20" IsActive="False"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="0" x:Name="ImageHost" Background="#FF161616" MinWidth="420" MinHeight="360">
            <Image x:Name="SourceImage" Stretch="Uniform"/>
            <Canvas x:Name="EvidenceCanvas" IsHitTestVisible="False"/>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="4">
                <ComboBox x:Name="ActionCombo" Header="Magic Action" DisplayMemberPath="Name" SelectionChanged="ActionCombo_SelectionChanged"/>
                <TextBlock x:Name="RecommendationText" TextWrapping="Wrap" Opacity="0.6"/>
            </StackPanel>
            <TextBox x:Name="QuestionBox" Grid.Row="1" Header="Question / instruction (optional)" PlaceholderText="Ask something specific about this capture" TextWrapping="Wrap"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                <Button x:Name="RunButton" Content="Run Magic Action" Click="Run_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click" IsEnabled="False"/>
                <Button Content="AI &amp; Magic settings" Click="Settings_Click"/>
            </StackPanel>

            <TextBox x:Name="ResultBox" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True" FontFamily="Segoe UI" PlaceholderText="Result appears here."/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8">
                <Button Content="Copy result" Click="CopyResult_Click"/>
                <Button Content="Copy JSON" Click="CopyJson_Click"/>
                <TextBlock x:Name="PayloadText" VerticalAlignment="Center" Opacity="0.6"/>
            </StackPanel>

            <Expander Grid.Row="5" Header="Evidence and context" IsExpanded="True">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Evidence" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <ListView x:Name="EvidenceList" SelectionChanged="EvidenceList_SelectionChanged" MaxHeight="150"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Context Stack" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <ListView x:Name="ContextList" DisplayMemberPath="SourceDisplayName" MaxHeight="115"/>
                        <Button Content="Clear context" Click="ClearContext_Click" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Expander>
        </Grid>

        <InfoBar x:Name="StatusBar" Grid.Row="2" Grid.ColumnSpan="2" IsOpen="False" IsClosable="True"/>
    </Grid>
</Window>
